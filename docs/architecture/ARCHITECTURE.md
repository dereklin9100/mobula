# 🏗️ Mobula 架构设计指南

> **Mobula 是一个基于 Turborepo 的现代化 Monorepo 架构，设计用于构建可扩展的多应用系统。**

**创建日期**: 2026-01-03  
**版本**: 1.0.0

---

## 📋 目录

1. [系统概览](#系统概览)
2. [核心架构](#核心架构)
3. [项目结构](#项目结构)
4. [依赖关系](#依赖关系)
5. [数据流](#数据流)
6. [技术栈](#技术栈)
7. [设计原则](#设计原则)
8. [开发工作流](#开发工作流)

---

## 系统概览

### 项目定位

**Mobula** 是一个生产级别的 Turborepo Monorepo 模板，支持：
- 📱 **多应用**: Web、Login、Dashboard 三个独立应用
- 🌍 **多语言**: 集成 next-intl 国际化
- 🎨 **设计系统**: 统一的 UI 组件库和主题系统
- 📦 **包管理**: 使用 pnpm workspaces 管理共享包

### 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    Mobula Monorepo                          │
│                    (Turborepo + pnpm)                       │
└─────────────────────────────────────────────────────────────┘
                              ▲
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────▼──────┐      ┌────────▼─────────┐  ┌──────▼──────┐
│   Web App    │      │   Login App      │  │ Dashboard   │
│  (Next.js)   │      │   (Next.js)      │  │  (Next.js)  │
│  Port: 3000  │      │   Port: 3001     │  │ Port: 3002  │
└───────┬──────┘      └────────┬─────────┘  └──────┬──────┘
        │                      │                    │
        └──────────────────────┼────────────────────┘
                               │
        ┌──────────────────────┼────────────────────┐
        │                      │                    │
┌───────▼──────────┐  ┌────────▼────────┐  ┌──────▼──────────┐
│ @repo/ui         │  │ @repo/intl      │  │ @repo/tailwind  │
│ (Component Lib)  │  │ (i18n Messages) │  │ (Theme Config)  │
└──────────────────┘  └─────────────────┘  └─────────────────┘
        │                      │                    │
        └──────────────────────┼────────────────────┘
                               │
        ┌──────────────────────┼────────────────────┐
        │                      │                    │
┌───────▼─────────┐  ┌────────▼──────────┐  ┌──────▼──────────┐
│ @repo/eslint    │  │ @repo/typescript  │  │ pnpm workspaces │
│ (Lint Config)   │  │ (TS Config)       │  │ (Package Mgmt)  │
└─────────────────┘  └───────────────────┘  └─────────────────┘
```

---

## 核心架构

### 1. Monorepo 架构 (Turborepo)

Mobula 采用 **Turborepo** 作为 Monorepo 构建系统，提供：

#### 优势
- ✅ **任务编排**: 自动依赖计算，快速缓存
- ✅ **增量构建**: 仅重新构建改变的包
- ✅ **远程缓存**: 支持分布式缓存，加速 CI/CD
- ✅ **并行执行**: 充分利用多核 CPU

#### 任务配置 (turbo.json)

```json
{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],        // 先构建依赖
      "outputs": [".next/**"],        // 输出产物
      "cache": true
    },
    "dev": {
      "cache": false,                 // dev 不缓存
      "persistent": true              // 持久运行
    }
  }
}
```

### 2. 包管理架构 (pnpm)

使用 **pnpm workspaces** 进行依赖管理：

```yaml
pnpm-workspace.yaml:
packages:
  - 'apps/*'      # 应用目录
  - 'packages/*'  # 共享包目录
```

#### 依赖解析
- `workspace:*` - 本地包内依赖（自动链接）
- `^1.0.0` - 外部 npm 依赖

---

## 项目结构

### 目录树

```
mobula/
├── apps/                          # 应用层
│   ├── web/                       # 主网站应用 (3000)
│   │   ├── app/                   # Next.js App Router
│   │   │   ├── layout.tsx         # 全局布局
│   │   │   └── page.tsx           # 主页面
│   │   ├── package.json
│   │   ├── next.config.ts
│   │   ├── tsconfig.json
│   │   └── tailwind.config.ts
│   │
│   ├── login/                     # 登录应用 (3001)
│   │   ├── app/
│   │   │   └── [locale]/          # i18n 动态路由
│   │   │       ├── layout.tsx
│   │   │       └── page.tsx
│   │   ├── i18n/                  # 国际化配置
│   │   │   ├── navigation.ts
│   │   │   ├── routing.ts
│   │   │   └── languages.ts
│   │   ├── messages/              # 翻译文件
│   │   │   ├── en.json
│   │   │   └── zh.json
│   │   └── proxy.ts               # 代理配置
│   │
│   └── dashboard/                 # 仪表板应用 (3002)
│       ├── app/
│       │   ├── layout.tsx
│       │   ├── page.tsx
│       │   └── data.json          # 示例数据
│       └── package.json
│
├── packages/                      # 共享包层
│   ├── ui/                        # UI 组件库
│   │   ├── src/
│   │   │   ├── components/        # shadcn/ui 组件
│   │   │   ├── hooks/             # 自定义 Hooks
│   │   │   ├── lib/               # 工具函数
│   │   │   └── index.ts           # 公开接口
│   │   └── package.json
│   │
│   ├── intl/                      # 国际化包
│   │   ├── src/
│   │   │   ├── messages/          # 翻译消息
│   │   │   ├── locales.ts         # 语言列表
│   │   │   └── index.ts           # 导出
│   │   ├── scripts/
│   │   │   └── sync.ts            # 消息同步脚本
│   │   └── package.json
│   │
│   ├── tailwind-config/           # Tailwind 配置包
│   │   ├── tailwind.config.ts     # 主题配置
│   │   ├── base.css               # 全局样式
│   │   └── package.json
│   │
│   ├── typescript-config/         # TypeScript 配置包
│   │   ├── base.json              # 基础配置
│   │   ├── nextjs.json            # Next.js 特定配置
│   │   └── package.json
│   │
│   └── eslint-config/             # ESLint 配置包
│       ├── base.js                # 基础规则
│       ├── next.js                # Next.js 规则
│       └── package.json
│
├── k8s/                           # Kubernetes 配置
│   ├── deployment.yaml            # K8s 部署配置
│   └── README.md                  # K8s 部署文档
│
├── docs/                          # 文档
│   ├── deployment/                # 部署文档
│   ├── architecture/              # 架构文档
│   └── ...
│
├── .github/
│   └── workflows/                 # GitHub Actions CI/CD
│       └── docker.yml
│
├── Dockerfile                     # 标准构建镜像
├── Dockerfile.prod                # 生产优化镜像
├── docker-compose.yml             # 开发环境编排
├── docker-compose.prod.yml        # 生产环境编排
├── package.json                   # 根工作区配置
├── pnpm-workspace.yaml            # pnpm 工作区配置
├── turbo.json                     # Turborepo 配置
└── tsconfig.json                  # 根 TypeScript 配置
```

### 应用层详解

#### 🌐 Web 应用 (Port 3000)
**用途**: 主网站，面向用户的公开应用
- 简洁的主页面
- 最小依赖配置
- 集成 UI 组件库

#### 🔐 Login 应用 (Port 3001)
**用途**: 认证和授权
- 完整的 i18n 支持（英文/中文）
- 动态路由 `[locale]`
- 国际化导航配置

#### 📊 Dashboard 应用 (Port 3002)
**用途**: 数据展示和管理
- 仪表板界面
- 数据可视化
- 管理功能

### 包层详解

#### 📦 @repo/ui
**UI 组件库**
- shadcn/ui 组件集合
- 自定义 Hooks
- 工具函数库
- 所有应用的统一 UI 基础

#### 📦 @repo/intl
**国际化管理包**
- 翻译消息管理
- 语言列表定义
- 同步脚本（将消息同步到各应用）

#### 📦 @repo/tailwind-config
**设计系统**
- 统一的 Tailwind 主题
- 全局样式和 CSS 变量
- 品牌色彩和排版设置

#### 📦 @repo/typescript-config
**TypeScript 配置标准化**
- 基础 TS 配置
- Next.js 特定配置
- 统一的编译目标和严格性设置

#### 📦 @repo/eslint-config
**代码质量管理**
- 统一的代码规则
- Next.js ESLint 最佳实践
- 自动格式化配置

---

## 依赖关系

### 依赖树

```
app:web
├── @repo/ui@workspace:*
│   ├── @radix-ui/react-*
│   ├── tailwind-merge
│   ├── class-variance-authority
│   └── ...
├── @repo/tailwind-config@workspace:*
│   └── tailwindcss@^4.1.18
├── next@16.1.0
└── react@^19.2.0

app:login
├── @repo/ui@workspace:*
├── @repo/intl@workspace:*
│   └── next-intl@^15.x
├── @repo/tailwind-config@workspace:*
├── next@16.1.0
└── react@^19.2.0

app:dashboard
├── @repo/ui@workspace:*
├── @repo/tailwind-config@workspace:*
├── next@16.1.0
├── react@^19.2.0
└── recharts@^3.6.0 (数据可视化)

@repo/ui
├── @radix-ui/react-avatar
├── @radix-ui/react-dialog
├── @radix-ui/react-tooltip
├── lucide-react (图标)
├── @tanstack/react-table (表格)
└── ...

@repo/intl
├── next-intl (国际化)
└── （消息文件）

@repo/tailwind-config
├── tailwindcss@^4.1.18
├── postcss@^8.5.6
└── autoprefixer@^10.4.23

@repo/typescript-config
└── typescript@5.9.2

@repo/eslint-config
└── eslint@^9.39.1
```

### 依赖隔离

```
根工作区
│
├── 应用层
│   ├── app:web (独立部署)
│   ├── app:login (独立部署)
│   └── app:dashboard (独立部署)
│
└── 包层 (共享)
    ├── @repo/ui (所有应用共享)
    ├── @repo/intl (login 应用强依赖)
    ├── @repo/tailwind-config (所有应用共享)
    ├── @repo/typescript-config (开发工具)
    └── @repo/eslint-config (开发工具)
```

---

## 数据流

### 1. 开发流程

```
开发者修改代码
    ↓
pnpm dev
    ↓
Turbo 分析依赖
    ↓
并行运行所有应用的 dev 任务
    ↓
Web: http://localhost:3000
Login: http://localhost:3001
Dashboard: http://localhost:3002
```

### 2. 构建流程

```
git push
    ↓
GitHub Actions 触发
    ↓
1. 运行 pnpm install (冻结依赖)
2. 运行 turbo build (按依赖顺序构建)
   ├── 构建 packages/* (共享包)
   └── 构建 apps/* (应用)
3. 构建 Docker 镜像 (多目标)
   ├── runner-web
   ├── runner-login
   └── runner-dashboard
4. 推送到 ghcr.io
```

### 3. 部署流程

#### Docker Compose (本地/小规模)
```
docker-compose up
    ↓
启动 3 个容器
├── web:3000
├── login:3001
└── dashboard:3002
```

#### Kubernetes (生产/大规模)
```
kubectl apply -f k8s/deployment.yaml
    ↓
创建 3 个 Deployment
├── web-deployment (2-10 副本)
├── login-deployment (2-10 副本)
└── dashboard-deployment (2-10 副本)
    ↓
HPA 自动扩容缩容
    ↓
负载均衡转发到各服务
```

---

## 技术栈

### 核心框架
| 技术 | 版本 | 用途 |
|------|------|------|
| Next.js | 16.1.0 | React 应用框架，SSR/SSG |
| React | 19.2.0 | UI 库 |
| Turborepo | 2.7.2 | Monorepo 构建系统 |
| TypeScript | 5.9.2 | 类型安全语言 |
| pnpm | 9.0.0 | 包管理器 |

### UI/样式
| 技术 | 版本 | 用途 |
|------|------|------|
| Tailwind CSS | 4.1.18 | 原子化 CSS 框架 |
| shadcn/ui | 最新 | Radix UI 组件库 |
| Radix UI | 最新 | 无头 UI 组件 |
| lucide-react | 0.562.0 | 图标库 |

### 国际化
| 技术 | 版本 | 用途 |
|------|------|------|
| next-intl | 15.x | Next.js 国际化 |

### 数据和图表
| 技术 | 版本 | 用途 |
|------|------|------|
| @tanstack/react-table | 8.21.3 | 高性能表格 |
| recharts | 3.6.0 | React 图表库 |

### 开发工具
| 技术 | 版本 | 用途 |
|------|------|------|
| ESLint | 9.39.1 | 代码质量检查 |
| Prettier | 3.7.4 | 代码格式化 |
| Docker | 最新 | 容器化 |

### 运维
| 技术 | 版本 | 用途 |
|------|------|------|
| Kubernetes | 1.19+ | 容器编排 |
| GitHub Actions | 内置 | CI/CD 自动化 |

---

## 设计原则

### 1. 关注点分离 (Separation of Concerns)

```
应用层 (App)
    ↓
    使用组件和 hooks
    ↓
包层 (Packages)
    ↓
    提供可复用的功能
```

**实践**:
- 每个应用独立部署、独立路由
- 共享包只提供通用功能，不关心具体业务
- UI 组件库无业务逻辑

### 2. 单一职责原则 (Single Responsibility)

**包的职责**:
- `@repo/ui` - 仅提供 UI 组件
- `@repo/intl` - 仅处理国际化
- `@repo/tailwind-config` - 仅管理设计系统
- `@repo/typescript-config` - 仅配置 TS 编译

### 3. DRY 原则 (Don't Repeat Yourself)

**共享**:
- 组件库
- 样式配置
- 类型定义
- 构建配置

**不共享**:
- 业务逻辑
- 数据源
- 路由结构

### 4. 可扩展性 (Scalability)

**支持增长**:
- 添加新应用 → 复制应用结构，复用共享包
- 添加新组件 → 在 @repo/ui 中开发
- 添加新语言 → 在 @repo/intl 中添加翻译

---

## 开发工作流

### 1. 本地开发

```bash
# 安装依赖
pnpm install

# 运行所有应用和包的开发服务
pnpm dev

# 结果：
# - Web: http://localhost:3000
# - Login: http://localhost:3001
# - Dashboard: http://localhost:3002

# 运行特定应用
pnpm --filter=web dev

# 运行特定包的任务
pnpm --filter=@repo/ui lint
```

### 2. 代码修改流

```
修改 @repo/ui 组件
    ↓
自动重新加载所有依赖它的应用
    ↓
无需重启，实时看到变化

修改 @repo/intl 消息
    ↓
运行同步脚本
    ↓
更新所有应用的翻译
```

### 3. 构建和部署

```bash
# 本地测试构建
pnpm build
turbo build

# 构建 Docker 镜像
docker build -t mobula:latest .

# 使用 Docker Compose 部署
docker-compose -f docker-compose.prod.yml up -d

# 使用 Kubernetes 部署
kubectl apply -f k8s/deployment.yaml
```

### 4. 质量检查

```bash
# 代码质量
pnpm lint

# 类型检查
pnpm check-types

# 格式化
pnpm format
```

---

## 性能考虑

### 1. 构建性能

- **Turborepo 缓存**: 避免重复构建，加快 CI/CD
- **增量构建**: 仅构建改变的包
- **并行任务**: 充分利用多核

### 2. 运行时性能

- **Code Splitting**: Next.js 自动代码分割
- **图片优化**: Next.js Image 组件
- **Lazy Loading**: 动态导入组件

### 3. 容器镜像优化

- **多阶段构建**: 分离 Builder 和 Runner
- **Alpine Linux**: 最小化镜像大小 (18MB 基础)
- **Layer Caching**: 优化 Docker 构建层

---

## 监控和日志

### 应用级监控
- Next.js 性能指标
- 错误捕获和报告
- 用户行为追踪

### 容器级监控
- CPU/内存使用率
- 网络流量
- 日志聚合

### Kubernetes 监控
- Pod 状态和日志
- HPA 扩容指标
- 服务可用性

---

## 总结

Mobula 架构的核心优势：

1. ✅ **Monorepo**: 统一管理多应用和共享包
2. ✅ **Turborepo**: 高效的任务编排和缓存
3. ✅ **类型安全**: 完整的 TypeScript 支持
4. ✅ **设计系统**: 统一的 UI 和样式
5. ✅ **国际化**: 内置 i18n 支持
6. ✅ **部署灵活**: Docker + Kubernetes
7. ✅ **可扩展**: 易于添加新应用和功能
8. ✅ **开发体验**: 快速反馈循环

---

**相关文档**:
- 📖 [部署指南](../deployment/README_DEPLOYMENT.md)
- 🐳 [Docker 指南](../deployment/DOCKER_GUIDE.md)
- ☸️ [Kubernetes 指南](../deployment/k8s/README.md)

